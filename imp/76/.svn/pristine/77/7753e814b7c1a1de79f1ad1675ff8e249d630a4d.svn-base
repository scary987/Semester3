#include <stdio.h>

/* Es wird angenommen, dass die eingegebene Zahl positiv und ganz ist. */
/* C ist Variable fuer den Rest. A ist die eingebene Zahl. B ist der Bool-Wert fuer Rest. */
int main()
{
	int a;
	int c;
	char bin[50];

	/* Schleife ueberarbeiten, die Null kommt im Rest oefter vor*/
	/* Andere Schlussbedingung festlegen*/
	while (/*c != 0*/)  // seid ihr Kinder behindert lest doch mal den compiler you cant comment in the while loop
	{
		a = a/2;

		if (a % 2 == 0) // Das mit dem == ist ja n netter Tip, aber das waren schlicht Anmerkungen...der soll garnichts compilen
		
		{
			/* Hat a durch 2 keinen Rest, speichere 0 */
			/* Realisierung via String-Funktionsoperationen */
			/* char null[1] = "0"; */
			/* strcat(null, bin); */
			/* null = " "; */
		}
		else
		{
			char one[1] = "1";
		}
	}

	printf("Die Binaere Darstellung fuer %f lautet %s.\n", a, bin);
}
