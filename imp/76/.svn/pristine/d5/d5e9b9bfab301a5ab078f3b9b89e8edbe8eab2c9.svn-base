#include <stdio.h>
#include <math.h>

/* Es wird angenommen, dass vom Nutzer nur sinnvolle
   Eingaben gemacht werden */

int main()
{
    /* Für die Berechnung des Sinus wird die Zahl Pi benötigt.
	   (Da nicht vorhanden eigenstndig definiert) */

    float Pi = 3.1415;
    double x;
	double sinus;
    double alpha;


    printf("Geben Sie den Wert im Bogenmass ein.\n");
    scanf("%lf", &x);

    /* Berechnung des Sinuswertes */

        alpha = (180/Pi)*x;

		sinus = sin((x*Pi)/180);

        printf("Winkel (Bogenmass) %f  Winkel (Gradmass) %f Sinuswert %f \n", x, alpha, sinus);

	/* Die Ausgabe der gesamten Reihenentwicklung wurde nicht umgesetzt. */

    return 0;
}
