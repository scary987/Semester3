#include <stdio.h> 
#include <stdlib.h>

/* 
Nr 4.
b)

Wie bestimme ich Zufällige Zahlen von 0 bis 1 und lege für eine unbestimmte
Anzahl von Feldern die proßentuale Wahrscheinlichkeit fest?

*/

int zufallsoutput a = float a[]{
int i = 0;
float sum = 0;
float rand = ()%1

while () {
if (sum < rand && sum + a[i] > rand)
{return i;
}
sum += a[i];
i ++;
}
return -1
}
