#include <stdio.h>
main() { /* Beispiel Vorlesungsfolie 9  */
	int a, b, s;
		
	printf("First number ? ");
	scanf("%d",&a);
	printf("Second number ? ");
	scanf("%d",&b);
	s = a+b;

	printf(" %d + %d = %d\n", a, b, s);

}
