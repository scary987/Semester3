#include <stdio.h>
main() {
	int w, x, y, z;
	printf("W? ");
	scanf("%d",&w);
	printf("X? ");
	scanf("%d",&x);
	y=1;
	while(y<=x) { y=y*w; printf("in Schleife y<=x y=%d\n",y); }
	y=y/w;
	while(y > 0) {
		printf("in Schleife y >0 x =%d, y=%d, z= %d \n",x,y,z);
		z = x/y;
		printf("Ausgabe laut Aufgabe %d\n",z);
		x = x - y*z;
		y = y/w;
	}
	printf("\n");
}